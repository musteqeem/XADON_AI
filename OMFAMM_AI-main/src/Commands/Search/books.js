const { fetchJson, inputText } = require('../_helpers');
module.exports={name:'books',alias:['book'],category:'Search',desc:'Search books with Open Library',usage:'.books <title or author>',execute:async(sock,m,{args,reply})=>{const q=inputText(args,m);if(!q)return reply('Usage: .books <title or author>');try{const d=await fetchJson(`https://openlibrary.org/search.json?q=${encodeURIComponent(q)}&limit=5`,{},15000);return reply(`📚 *Books: ${q}*\\n\\n${(d.docs||[]).map((x,i)=>`${i+1}. ${x.title}\\n${x.author_name?.slice(0,3).join(', ')||'Unknown author'}\\nhttps://openlibrary.org${x.key||''}`).join('\\n\\n')||'No books found.'}`)}catch(e){return reply(`Book search failed: ${e.message}`)}}};
