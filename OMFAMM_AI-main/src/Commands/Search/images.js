const { fetchJson, inputText } = require('../_helpers');
module.exports={name:'images',alias:['imgsearch'],category:'Search',desc:'Find image search results without downloading them',usage:'.images <query>',execute:async(sock,m,{args,reply})=>{const q=inputText(args,m);if(!q)return reply('Usage: .images <query>');try{const d=await fetchJson(`https://api.duckduckgo.com/?q=${encodeURIComponent(q)}&format=json&no_html=1&iax=images&ia=images`,{},15000);const topics=(d.RelatedTopics||[]).flatMap(x=>x.Topics||x).filter(x=>x.FirstURL||x.Icon?.URL).slice(0,5);return reply(`🖼️ *Image search: ${q}*\\n\\n${topics.map((x,i)=>`${i+1}. ${x.Text||'Result'}\\n${x.FirstURL||''}`).join('\\n\\n')||`Search directly: https://duckduckgo.com/?iax=images&ia=images&q=${encodeURIComponent(q)}`}`)}catch(e){return reply(`Image search failed: ${e.message}`)}}};
