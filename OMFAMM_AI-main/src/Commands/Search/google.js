const { fetchJson, inputText } = require('../_helpers');
module.exports={name:'google',alias:['search','ddg'],category:'Search',desc:'Search the web using DuckDuckGo instant answers',usage:'.google <query>',execute:async(sock,m,{args,reply})=>{const q=inputText(args,m);if(!q)return reply('Usage: .google <query>');try{const d=await fetchJson(`https://api.duckduckgo.com/?q=${encodeURIComponent(q)}&format=json&no_html=1&skip_disambig=1`,{},15000);const related=(d.RelatedTopics||[]).flatMap(x=>x.Topics||x).filter(x=>x.Text).slice(0,4);return reply(`🔎 *${q}*\\n\\n${d.AbstractText||d.Answer||related.map(x=>`• ${x.Text}`).join('\\n')||'No instant answer found.'}${d.AbstractURL?`\\n\\n${d.AbstractURL}`:''}`)}catch(e){return reply(`Search failed: ${e.message}`)}}};
