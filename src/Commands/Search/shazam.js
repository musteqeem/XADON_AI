const { fetchJson, inputText } = require('../_helpers');
module.exports={name:'shazam',alias:['songinfo'],category:'Search',desc:'Search music metadata using iTunes',usage:'.shazam <song or artist>',execute:async(sock,m,{args,reply})=>{const q=inputText(args,m);if(!q)return reply('Usage: .shazam <song or artist>');try{const d=await fetchJson(`https://itunes.apple.com/search?term=${encodeURIComponent(q)}&entity=song&limit=5`,{},15000);return reply(`🎵 *Music search: ${q}*\\n\\n${(d.results||[]).map((x,i)=>`${i+1}. ${x.trackName}\\n${x.artistName} — ${x.collectionName||'Single'}\\n${x.trackViewUrl||''}`).join('\\n\\n')||'No tracks found.'}`)}catch(e){return reply(`Music search failed: ${e.message}`)}}};
