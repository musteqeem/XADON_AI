const { fetchJson, inputText } = require('../_helpers');
module.exports={name:'fxconvert',alias:['convertcurrency'],category:'Search',desc:'Convert currencies using Frankfurter rates',usage:'.fxconvert <amount> <from> <to>',execute:async(sock,m,{args,reply})=>{const [amount,from,to]=args||[];const n=Number(amount);if(!Number.isFinite(n)||!from||!to)return reply('Usage: .fxconvert <amount> <from> <to>');try{const d=await fetchJson(`https://api.frankfurter.app/latest?amount=${n}&from=${encodeURIComponent(from.toUpperCase())}&to=${encodeURIComponent(to.toUpperCase())}`,{},15000);const val=d.rates?.[to.toUpperCase()];if(val==null)return reply('Currency pair not available.');return reply(`💱 ${n} ${from.toUpperCase()} = ${val} ${to.toUpperCase()}\\nRate date: ${d.date}`)}catch(e){return reply(`Currency conversion failed: ${e.message}`)}}};
